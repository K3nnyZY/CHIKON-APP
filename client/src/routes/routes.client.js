import {ClientLayout} from "../layouts"
import {Home} from "../pages/Client/"

const routesClient = [
    {
        path: "/",
        component: Home,
        layout: ClientLayout,
    }

]

export default routesClient